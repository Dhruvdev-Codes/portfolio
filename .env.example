# Optional: Upstash Vector credentials for production RAG vector search
UPSTASH_VECTOR_REST_URL=""
UPSTASH_VECTOR_REST_TOKEN=""

# Optional: OpenAI API Key for GPT-4o-mini streaming completions
OPENAI_API_KEY=""

# Note: The application includes an in-memory keyword-matched stream fallback.
# If these variables are not set, the interactive AI Assistant will work immediately out-of-the-box!
